/**
 * Auth Controller Hook
 * Connects UI views to AuthContext, actions, and services.
 */
export { useAuth } from '@/context/auth-context';
export type { AuthContextType } from '@/context/auth-context';
